# 환경 변수 및 비밀
.env
.env.local
*.env
!.env.example

# API 키 / 자격증명
secrets/
credentials.json
service-account.json
# Google 서비스 계정 키 (HIVE-35) — 실제 키 파일명(google_service_account.json) 및 변형 차단
google_service_account.json
*service_account*.json
*service-account*.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
ENV/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 의존성
node_modules/

# 빌드 산출물
dist/
build/
*.egg

# 크롤링 데이터 (DB에 적재, git에 올리지 않음)
data/raw/
data/cache/
data/*.json
data/*.csv

# DB
*.db
*.sqlite
*.sqlite3

# 모델 체크포인트 (Colab/외부 저장)
*.pt
*.pth
*.ckpt
models/checkpoints/

# Jupyter
.ipynb_checkpoints/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# 로그
*.log
logs/

# Streamlit
.streamlit/secrets.toml

# GraphSAGE 학습 산출물 (DB에서 재생산 가능 — git에 올리지 않음)
backend/data/*.npz

# 맥미니 배포 시크릿
deploy/macmini/.env

# 로컬 전용 메모/기획 노트 (GitHub push 금지)
DEV-HIVE/

# 크롤 결과 덤프 (아티팩트 — 커밋 금지, DB/드라이브에 적재)
backend/*_only.json
backend/x_crawl_result*.json
